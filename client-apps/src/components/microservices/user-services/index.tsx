import { Alert } from "@/components/ui/alerts";
import React from "react";

export default function UserManagement() {
  return (
    <div>
      <Alert
        message="Please proceed with creating the User Management functionality using the RESTful API from the previously developed module."
        variant="info"
      />
    </div>
  );
}
